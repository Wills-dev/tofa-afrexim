const InputDescription = ({ description }: { description: string }) => {
  return (
    <p className="text-xs font-extralight text-green-800">{description}</p>
  );
};

export default InputDescription;
